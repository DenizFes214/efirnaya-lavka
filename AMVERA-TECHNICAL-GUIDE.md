# Техническое руководство по развертыванию на AMVERA Cloud

> **Версия документа:** 1.0  
> **Дата создания:** 17 ноября 2025  
> **Основано на:** [Официальная документация AMVERA](https://docs.amvera.ru/)

---

## 📋 Содержание

1. [Введение](#введение)
2. [Быстрый старт](#быстрый-старт)
3. [Структура хранилища](#структура-хранилища)
4. [Файл конфигурации amvera.yml](#файл-конфигурации-amverayml)
5. [Сетевое взаимодействие](#сетевое-взаимодействие)
6. [Переменные окружения и секреты](#переменные-окружения-и-секреты)
7. [Работа с SQLite](#работа-с-sqlite)
8. [Процесс сборки и деплоя](#процесс-сборки-и-деплоя)
9. [Типичные ошибки и решения](#типичные-ошибки-и-решения)
10. [Лучшие практики](#лучшие-практики)

---

## Введение

**AMVERA Cloud** — это российская платформа для развертывания приложений с автоматической сборкой из Git-репозитория. Платформа поддерживает:

- ✅ Docker-контейнеры
- ✅ Стандартные окружения (Node.js, Python, Go, и др.)
- ✅ Автоматический деплой при push в master
- ✅ Бесплатные домены `*.amvera.io`
- ✅ SSL-сертификаты Let's Encrypt
- ✅ Встроенную систему секретов

---

## Быстрый старт

### 1. Создание приложения

1. Зарегистрируйтесь на [amvera.ru](https://amvera.ru/)
2. Создайте новое приложение в личном кабинете
3. Привяжите Git-репозиторий (GitHub, GitLab, Bitbucket)
4. Настройте файл `amvera.yml` (см. ниже)

### 2. Инициализация Git

```bash
git init
git remote add origin <URL_вашего_репозитория>
git add .
git commit -m "Initial commit"
git push -u origin master
```

### 3. Автоматический деплой

После push в ветку `master` AMVERA автоматически:
- Клонирует репозиторий
- Запускает сборку (build)
- Создает контейнер
- Запускает приложение (run)

---

## Структура хранилища

AMVERA использует **3 типа папок**:

### 📁 Code (Репозиторий)
- **Назначение:** Git-контролируемые файлы
- **Путь:** `/app` (внутри контейнера)
- **Особенности:**
  - Синхронизируется с Git при каждом push
  - Не подходит для хранения данных БД
  - Используется для кода приложения

### 📁 Artifacts (Артефакты сборки)
- **Назначение:** Результаты сборки
- **Путь:** Зависит от языка/окружения
- **Особенности:**
  - ⚠️ **УСТАРЕЛО** - не рекомендуется для хранения данных
  - Может быть перезаписана при пересборке
  - Удаляется при обновлении кода

### 📁 Data (Постоянное хранилище)
- **Назначение:** Персистентные данные (БД, файлы)
- **Путь:** `/data` (по умолчанию)
- **Особенности:**
  - ✅ **РЕКОМЕНДУЕТСЯ** для SQLite, загрузок, логов
  - Сохраняется между деплоями
  - Можно изменить путь через `persistenceMount`
  - Доступ через интерфейс AMVERA (загрузка/скачивание)

---

## Файл конфигурации amvera.yml

### ⚠️ КРИТИЧЕСКИ ВАЖНО: Два формата конфигурации

AMVERA поддерживает **2 разных формата** конфигурации:

#### 1️⃣ Формат для Docker (meta.environment: docker)

```yaml
meta:
  environment: docker
  toolchain:
    name: ""
    version: ""

build:
  dockerfile:
    path: Dockerfile

run:
  containerPort: 80
  servicePort: 80
  persistenceMount: /data
```

#### 2️⃣ Формат для стандартных окружений (Node.js, Python и др.)

```yaml
meta:
  environment: node
  toolchain:
    name: npm
    version: 18

build:
  cmds:
    - npm install --production

run:
  command: node
  args:
    - server.js
```

### ❌ Типичная ошибка

**НЕ смешивайте форматы!** Ошибка приводит к 404:

```yaml
# ❌ НЕПРАВИЛЬНО - это вызовет ошибку
meta:
  environment: docker  # Docker формат
  
run:
  command: node        # Команды из стандартного формата
  args:                # НЕ работают с Docker!
    - server.js
```

### ✅ Правильная конфигурация для Docker

```yaml
meta:
  environment: docker
  toolchain:
    name: ""
    version: ""

build:
  dockerfile:
    path: Dockerfile

run:
  containerPort: 80      # Порт внутри контейнера
  servicePort: 80        # Порт для доступа
  persistenceMount: /data # Папка для постоянных данных
```

### 📝 Параметры секции `run`

| Параметр | Описание | Значение по умолчанию |
|----------|----------|----------------------|
| `containerPort` | Порт, который слушает приложение | `80` |
| `servicePort` | Порт для внешнего доступа | `80` |
| `persistenceMount` | Путь для постоянных данных | `/data` |

### 💡 Переменная окружения PORT

Вместо явного указания порта можно использовать:

```javascript
const PORT = process.env.PORT || 3000;
app.listen(PORT);
```

По умолчанию `PORT=80` в AMVERA.

---

## Сетевое взаимодействие

### 🌐 Доступ из интернета

#### Бесплатный домен AMVERA

Формат: `<транслит-имя-проекта>.<имя-пользователя>.amvera.io`

**Пример:** `efirnayalavka-aleksei57.amvera.io`

**Особенности:**
- ✅ Автоматический SSL (Let's Encrypt)
- ✅ HTTP/HTTPS поддержка
- ✅ Настраивается в разделе «Настройки» → «Доменные имена»

#### Собственный домен

1. В личном кабинете регистратора добавьте:
   - **A запись:** IP от AMVERA (указан в настройках)
   - **TXT запись:** Проверочная запись от AMVERA
2. Подождите распространения DNS (до 24 часов)
3. В AMVERA: «Настройки» → «Создать доменное имя» → «Подтвердить и привязать»

#### Проверка DNS записей

Используйте [MXToolbox](https://mxtoolbox.com/txtlookup.aspx):
- TXT Lookup → должна появиться запись AMVERA
- DNS Lookup → должен быть IP AMVERA

### 🔗 Внутреннее взаимодействие

Формат: `amvera-<транслит-имя>-run-<пользователь>`

**Пример:** `amvera-efirnayalavka-run-aleksei57`

⚠️ **Важно:** Внутренняя сеть **не шифруется** AMVERA!

### 🚪 Настройка портов

Для нескольких портов используйте запятую:

```yaml
run:
  containerPort: 8090,3000  # Порты внутри контейнера
  servicePort: 90,3000      # Порты для доступа
```

**Пример маршрутизации:**
- `domain.amvera.io/web` → порт 80
- `domain.amvera.io/backend` → порт 8080

### 🔌 TCP трафик

Для баз данных доступны специальные типы подключения:

| Тип | Порт | Домен |
|-----|------|-------|
| POSTGRES | 5432 | `*.db-<region>.amvera.tech` |
| MONGO | 27017 | `*.db-<region>.amvera.tech` |
| REDIS | 6379 | `*.db-<region>.amvera.tech` |

---

## Переменные окружения и секреты

### 📝 Добавление переменных

1. Перейдите в раздел «Переменные»
2. Нажмите «Создать переменную»
3. Укажите **Название** и **Значение**
4. ⚠️ **Перезапустите контейнеры** для применения

### 💻 Доступ к переменным

**JavaScript (Node.js):**
```javascript
const myVar = process.env.MY_VAR;
```

**Python:**
```python
import os
my_var = os.environ["MY_VAR"]
```

### 🔐 Отличие секретов от переменных

| Параметр | Переменная | Секрет |
|----------|-----------|--------|
| Хранение | Открытый текст в БД | Отдельное хранилище |
| Видимость | Видны в интерфейсе | Скрыты |
| Использование | Настройки | Токены, пароли |

### 🏷️ Системная переменная AMVERA

Переменная `AMVERA=1` автоматически доступна в облаке:

```javascript
if (process.env.AMVERA === "1") {
  console.log("Работаю в AMVERA Cloud");
} else {
  console.log("Работаю локально");
}
```

### ⚠️ Важные ограничения

- **НЕ доступны** на этапе сборки (build)
- Доступны только при запуске (run)
- Требуют перезапуска контейнеров после изменения

---

## Работа с SQLite

### 📂 Правильное размещение БД

✅ **ПРАВИЛЬНО:**
```javascript
const dbPath = '/data/database.db';  // В постоянном хранилище
```

❌ **НЕПРАВИЛЬНО:**
```javascript
const dbPath = './database.db';      // В папке Artifacts - ПОТЕРЯЕТСЯ!
const dbPath = 'data/database.db';   // Относительный путь - НЕНАДЕЖНО!
```

### 📝 Примеры подключения

#### JavaScript (Node.js) - sqlite3

```javascript
const sqlite3 = require('sqlite3').verbose();

const db = new sqlite3.Database(
  '/data/example.db',
  sqlite3.OPEN_READWRITE | sqlite3.OPEN_CREATE,
  (err) => {
    if (err) {
      console.error(err.message);
      return;
    }
    console.log('SQLite подключена успешно');
  }
);

// Использование
db.run('CREATE TABLE IF NOT EXISTS users (id INTEGER PRIMARY KEY, name TEXT)');

// Закрытие
db.close();
```

#### JavaScript (Node.js) - better-sqlite3

```javascript
const Database = require('better-sqlite3');

const db = new Database('/data/example.db', {
  verbose: console.log
});

// Использование
const stmt = db.prepare('INSERT INTO users (name) VALUES (?)');
stmt.run('Алексей');

// Закрытие
db.close();
```

#### Python

```python
import sqlite3

# Подключение
conn = sqlite3.connect('/data/example.db')
cursor = conn.cursor()

# Использование
cursor.execute('CREATE TABLE IF NOT EXISTS users (id INTEGER PRIMARY KEY, name TEXT)')
conn.commit()

# Закрытие
conn.close()
```

### 🛠️ Локальное тестирование

Создайте папку `/data` на локальном компьютере:

**Linux/macOS:**
```bash
sudo mkdir -p /data
sudo chown $USER:$USER /data
```

**Windows:**
```powershell
# Создайте папку C:\data или используйте путь с переменной
```

**Универсальное решение:**
```javascript
const dbPath = process.env.AMVERA === "1" 
  ? '/data/database.db'      // В AMVERA
  : './data/database.db';    // Локально
```

### 📤 Загрузка существующей БД

1. Перейдите в раздел «Репозиторий»
2. Выберите папку **Data**
3. Загрузите файл `.db` через интерфейс

⚠️ **Важно:** Папка `data` в корне репозитория и `/data` в контейнере — **это разные места!**

---

## Процесс сборки и деплоя

### 🔄 Автоматический процесс

1. **Git Push** → ветка `master` (или настроенная)
2. **Clone** → AMVERA клонирует код в папку Code
3. **Build** → Выполняется сборка:
   - Для Docker: `docker build -f Dockerfile`
   - Для стандартных: команды из секции `build.cmds`
4. **Run** → Запуск контейнера с параметрами из секции `run`

### 📋 Логи сборки

Доступны в разделе «Логи»:
- **Build logs** — логи сборки
- **Runtime logs** — логи работы приложения

### 🔍 Проверка состояния

```bash
# Локально перед push
git status
git log --oneline -5

# После push
# Проверьте логи в интерфейсе AMVERA
```

### ⏱️ Время развертывания

- **Сборка:** 2-10 минут (зависит от сложности)
- **Выдача SSL:** До 5 минут
- **DNS propagation:** До 24 часов (для собственного домена)

---

## Типичные ошибки и решения

### ❌ Ошибка: 404 Page Not Found

**Причина 1:** Неправильный формат `amvera.yml`

```yaml
# ❌ НЕПРАВИЛЬНО
meta:
  environment: docker
run:
  command: node      # Эти параметры для стандартных окружений!
  args:
    - server.js
```

**Решение:**
```yaml
# ✅ ПРАВИЛЬНО
meta:
  environment: docker
build:
  dockerfile:
    path: Dockerfile
run:
  containerPort: 80
```

**Причина 2:** Приложение не слушает порт 80

```javascript
// ❌ НЕПРАВИЛЬНО
app.listen(3000);  // Жестко заданный порт
```

**Решение:**
```javascript
// ✅ ПРАВИЛЬНО
const PORT = process.env.PORT || 80;
app.listen(PORT);
```

### ❌ Ошибка: База данных пустая после деплоя

**Причина:** БД сохраняется не в `/data`

```javascript
// ❌ НЕПРАВИЛЬНО
const db = new Database('./database.db');  // Относительный путь
```

**Решение:**
```javascript
// ✅ ПРАВИЛЬНО
const db = new Database('/data/database.db');  // Абсолютный путь в Data
```

### ❌ Ошибка: Healthcheck failed

**Причина:** curl не установлен в Docker-образе

```dockerfile
# ❌ НЕПРАВИЛЬНО
RUN apt-get update && apt-get install -y python3 make g++
```

**Решение:**
```dockerfile
# ✅ ПРАВИЛЬНО
RUN apt-get update && apt-get install -y curl python3 make g++
```

### ❌ Ошибка: Переменные окружения не работают

**Причина:** Переменные не доступны на этапе build

```yaml
build:
  cmds:
    - echo $MY_SECRET  # ❌ НЕ СРАБОТАЕТ
```

**Решение:**
- Переменные доступны только при **run**
- Для build используйте другие механизмы

⚠️ **После добавления переменных:** Обязательно перезапустите контейнеры!

### ❌ Ошибка: SSL сертификат не выдается

**Причина:** DNS записи не распространились

**Решение:**
1. Проверьте записи через [MXToolbox](https://mxtoolbox.com/)
2. Подождите до 24 часов
3. Проверьте, что A запись указывает на IP AMVERA
4. Убедитесь, что TXT запись совпадает с требуемой

---

## Лучшие практики

### ✅ Структура проекта

```
project/
├── amvera.yml           # Конфигурация AMVERA
├── Dockerfile           # Docker образ
├── docker-compose.yml   # Для локальной разработки
├── .gitignore           # Исключения Git
├── package.json         # Зависимости Node.js
├── backend/
│   ├── server.js
│   └── db.js            # Подключение к БД
└── frontend/
    └── index.html
```

### ✅ .gitignore

```gitignore
node_modules/
.env
*.db              # БД не в Git!
.DS_Store
npm-debug.log
```

### ✅ Безопасность

1. **Секреты** — только через интерфейс AMVERA
2. **Не коммитьте:**
   - API ключи
   - Пароли
   - Токены
   - Файлы .env
3. **Используйте:**
   - Секреты вместо переменных для паролей
   - HTTPS для внешнего доступа
   - Внутреннюю сеть только для доверенных сервисов

### ✅ Производительность

1. **Dockerfile:**
   - Используйте slim образы (`node:18-slim`)
   - Многоступенчатая сборка для уменьшения размера
   - Кэшируйте зависимости

```dockerfile
# ✅ Хороший пример
FROM node:18-bullseye-slim

# Кэшируется при неизменном package.json
COPY package*.json ./
RUN npm ci --production

# Копируем код последним
COPY . .
```

2. **База данных:**
   - SQLite для малых нагрузок (< 1000 RPS)
   - PostgreSQL для больших нагрузок
   - Регулярные бэкапы через интерфейс

### ✅ Мониторинг

1. **Логирование:**
```javascript
console.log('Server started:', {
  port: PORT,
  environment: process.env.AMVERA ? 'production' : 'development',
  timestamp: new Date().toISOString()
});
```

2. **Healthcheck в Dockerfile:**
```dockerfile
HEALTHCHECK --interval=30s --timeout=3s --start-period=40s --retries=3 \
  CMD curl -f http://localhost:80/health || exit 1
```

3. **Эндпоинт проверки:**
```javascript
app.get('/health', (req, res) => {
  res.status(200).json({ 
    status: 'ok', 
    uptime: process.uptime(),
    timestamp: Date.now()
  });
});
```

### ✅ Разработка

1. **Локальное тестирование:**
```bash
# Создайте /data локально
mkdir -p /data  # Linux/macOS
# или C:\data  # Windows

# Запустите Docker локально
docker-compose up

# Тестируйте с теми же переменными
docker run -e PORT=80 -e AMVERA=1 -v /data:/data myapp
```

2. **Используйте docker-compose.yml:**
```yaml
version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:80"
    volumes:
      - ./data:/data
    environment:
      - PORT=80
      - NODE_ENV=development
```

### ✅ Git workflow

```bash
# 1. Разработка в отдельной ветке
git checkout -b feature/new-feature
git add .
git commit -m "Add new feature"
git push origin feature/new-feature

# 2. Merge в master после тестирования
git checkout master
git merge feature/new-feature

# 3. Push запускает деплой
git push origin master

# 4. Проверьте логи в AMVERA
```

---

## 📚 Дополнительные ресурсы

- **Документация AMVERA:** [docs.amvera.ru](https://docs.amvera.ru/)
- **Примеры проектов:** [docs.amvera.ru/general/examples.html](https://docs.amvera.ru/general/examples.html)
- **FAQ:** [docs.amvera.ru/general/faq.html](https://docs.amvera.ru/general/faq.html)
- **CLI управление:** [docs.amvera.ru/general/cli.html](https://docs.amvera.ru/general/cli.html)
- **Тарифы:** [docs.amvera.ru/general/price.html](https://docs.amvera.ru/general/price.html)

---

## 📧 Поддержка

- **Email:** support@amvera.ru
- **Telegram:** @amvera_support
- **Документация:** [docs.amvera.ru](https://docs.amvera.ru/)

---

## 📝 История изменений

| Дата | Версия | Изменения |
|------|--------|-----------|
| 17.11.2025 | 1.0 | Первая версия документа |

---

**© 2024 AMVERA Cloud | Документ создан на основе официальной документации**
