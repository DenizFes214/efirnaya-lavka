# 🔧 Настройка переменных окружения в AMVERA

> **Дата:** 17 ноября 2025  
> **Проблема:** 404 из-за неправильной конфигурации переменных окружения

---

## ⚠️ КРИТИЧЕСКИ ВАЖНО

В Docker формате AMVERA **переменные окружения НЕ добавляются через amvera.yml**!

Их нужно добавить **через веб-интерфейс AMVERA** в разделе "Переменные".

---

## 📋 Список обязательных переменных

### 1. Системные переменные

| Название | Значение | Описание |
|----------|----------|----------|
| `NODE_ENV` | `production` | Режим работы Node.js |
| `PORT` | `80` | Порт приложения |

### 2. Публичный URL

| Название | Значение | Описание |
|----------|----------|----------|
| `PUBLIC_URL` | `https://efirnayalavka-aleksei57.amvera.io` | Публичный адрес приложения |

### 3. Telegram Bot (СЕКРЕТ!)

| Название | Значение | Описание |
|----------|----------|----------|
| `BOT_TOKEN` | `8340741653:AAGFC-nW1BnLobjhgXSKRjNY83HkU4pCqrw` | Токен Telegram бота |

⚠️ **Это СЕКРЕТ** - добавляйте как "Секрет", а не "Переменная"!

### 4. Администраторы

| Название | Значение | Описание |
|----------|----------|----------|
| `ADMIN_IDS` | `985246360,1562870920` | ID админов через запятую |

### 5. Telegram каналы

| Название | Значение | Описание |
|----------|----------|----------|
| `MAIN_CHANNEL_ID` | `-1002261187486` | Основной канал (@grimuar_aroma) |
| `TEST_CHANNEL_ID` | `-1002277761715` | Тестовый канал |

### 6. Пути к данным

| Название | Значение | Описание |
|----------|----------|----------|
| `DB_PATH` | `/data/efirnaya-lavka.sqlite` | Путь к базе данных |
| `UPLOADS_PATH` | `/data/uploads` | Папка для загрузок |

### 7. Платежи (необязательно для старта)

| Название | Значение | Описание |
|----------|----------|----------|
| `YOOKASSA_SHOP_ID` | `test_shop_id` | ID магазина YooKassa |
| `YOOKASSA_SECRET_KEY` | `test_secret_key` | Секретный ключ YooKassa |

⚠️ **Это СЕКРЕТЫ** - добавляйте как "Секрет"!

---

## 🎯 Инструкция по добавлению в AMVERA

### Шаг 1: Откройте раздел "Переменные"

1. Войдите в панель управления AMVERA
2. Выберите проект `efirnayalavka`
3. Перейдите в раздел **"Переменные"**

### Шаг 2: Добавьте обычные переменные

Нажмите **"Создать переменную"** и добавьте по одной:

```
Название: NODE_ENV
Значение: production
Тип: Переменная окружения
```

```
Название: PORT
Значение: 80
Тип: Переменная окружения
```

```
Название: PUBLIC_URL
Значение: https://efirnayalavka-aleksei57.amvera.io
Тип: Переменная окружения
```

```
Название: ADMIN_IDS
Значение: 985246360,1562870920
Тип: Переменная окружения
```

```
Название: MAIN_CHANNEL_ID
Значение: -1002261187486
Тип: Переменная окружения
```

```
Название: TEST_CHANNEL_ID
Значение: -1002277761715
Тип: Переменная окружения
```

```
Название: DB_PATH
Значение: /data/efirnaya-lavka.sqlite
Тип: Переменная окружения
```

```
Название: UPLOADS_PATH
Значение: /data/uploads
Тип: Переменная окружения
```

### Шаг 3: Добавьте секреты

Нажмите **"Создать переменную"** и выберите тип **"Секрет"**:

```
Название: BOT_TOKEN
Значение: 8340741653:AAGFC-nW1BnLobjhgXSKRjNY83HkU4pCqrw
Тип: Секрет
```

```
Название: YOOKASSA_SHOP_ID
Значение: test_shop_id
Тип: Секрет
```

```
Название: YOOKASSA_SECRET_KEY
Значение: test_secret_key
Тип: Секрет
```

### Шаг 4: Перезапустите контейнеры

⚠️ **ОБЯЗАТЕЛЬНО!** После добавления всех переменных:

1. Нажмите кнопку **"Перезапустить"** в панели управления
2. Дождитесь полного перезапуска (1-2 минуты)
3. Проверьте логи - должны увидеть "🌿 Эфирная Лавка запущена"

---

## 🌐 Проверка публичного URL

После настройки переменных и перезапуска:

### 1. Проверьте доменное имя

В разделе **"Настройки"** → **"Доменные имена"**:

- Должен быть добавлен домен: `efirnayalavka-aleksei57.amvera.io`
- Тип подключения: **HTTPS**
- Статус: **Активен** ✅

### 2. Если домена нет - добавьте его

1. Нажмите **"Добавить доменное имя"**
2. Выберите **"Бесплатный домен Амвера"**
3. Тип подключения: **HTTPS**
4. Домен создастся автоматически в формате: `<имя-проекта>.<пользователь>.amvera.io`

### 3. Проверьте доступность

Откройте в браузере:
```
https://efirnayalavka-aleksei57.amvera.io
```

Должна открыться главная страница "Эфирной Лавки" 🌿

### 4. Проверьте API

```bash
curl https://efirnayalavka-aleksei57.amvera.io/api/health
```

Должен вернуть:
```json
{"ok":true,"timestamp":"2025-11-17T..."}
```

---

## 🔍 Диагностика проблем

### Проблема: 404 после добавления переменных

**Причина:** Контейнеры не перезапущены

**Решение:**
1. Раздел "Настройки" → кнопка "Перезапустить"
2. Проверьте логи - должно быть "🌿 Эфирная Лавка запущена"

### Проблема: Приложение не запускается

**Причина:** Отсутствуют обязательные переменные

**Решение:**
1. Проверьте логи - там будет ошибка о недостающей переменной
2. Добавьте недостающую переменную
3. Перезапустите

### Проблема: SSL сертификат не работает

**Причина:** Домен еще не настроен или сертификат не выдан

**Решение:**
1. Проверьте "Доменные имена" - должен быть статус "Активен"
2. Подождите 5-10 минут для выдачи SSL
3. Если не помогло - удалите и добавьте домен заново

---

## 📝 Быстрая проверка

После всех настроек выполните:

```bash
# 1. Healthcheck
curl https://efirnayalavka-aleksei57.amvera.io/api/health

# 2. Категории
curl https://efirnayalavka-aleksei57.amvera.io/api/categories

# 3. Главная страница
curl https://efirnayalavka-aleksei57.amvera.io
```

Все три запроса должны вернуть корректные данные (не 404).

---

## ✅ Что должно получиться

После правильной настройки:

1. ✅ Приложение запущено (логи: "🌿 Эфирная Лавка запущена")
2. ✅ Домен активен (https://efirnayalavka-aleksei57.amvera.io)
3. ✅ SSL работает (иконка замка в браузере)
4. ✅ API отвечает (healthcheck возвращает ok)
5. ✅ Главная страница открывается
6. ✅ База данных работает (категории загружаются)

---

## 🆘 Если ничего не помогло

1. **Проверьте формат amvera.yml:**
   - Должен быть Docker формат (без секции `env:`)
   - Смотрите `AMVERA-TECHNICAL-GUIDE.md` → раздел "Файл конфигурации"

2. **Пересоберите проект:**
   ```bash
   git add .
   git commit -m "fix: remove env from amvera.yml"
   git push amvera main
   ```

3. **Проверьте логи сборки:**
   - Должно быть "INFO[...] Pushed cr.yandex/..."
   - Не должно быть ошибок

4. **Свяжитесь с поддержкой AMVERA:**
   - Email: support@amvera.ru
   - Telegram: @amvera_support

---

**🌿 После правильной настройки Эфирная Лавка заработает! ✨**
