import { Telegraf } from 'telegraf';
import dotenv from 'dotenv';
dotenv.config();

// Конфигурация бота Эфирной Лавки
const BOT_TOKEN = process.env.BOT_TOKEN || '8340741653:AAGFC-nW1BnLobjhgXSKRjNY83HkU4pCqrw';
const PUBLIC_URL = process.env.PUBLIC_URL || 'https://your-domain.amvera.io';

// Создаём экземпляр бота
const bot = new Telegraf(BOT_TOKEN);

// При команде /start отправляем магическое приветствие
bot.start((ctx) => {
    const userName = ctx.from.first_name || 'Путешественник';
    
    ctx.reply(`🌟 Приветствую тебя, ${userName}, в 🌿 *Эфирной Лавке* 🌿
    
🕯️ Добро пожаловать в мир древней мудрости и магических ароматов!

Здесь ты найдешь:
� Редчайшие эфирные масла
🍃 Священные травы и сборы  
🔮 Ритуальные предметы силы
📜 Древние знания и руководства
✨ Магические услуги и консультации

Открой врата в наш мистический магазин...`, {
        parse_mode: 'Markdown',
        reply_markup: {
            keyboard: [
                [
                    {
                        text: '🌿 Войти в Эфирную Лавку ✨',
                        web_app: { url: PUBLIC_URL }
                    }
                ],
                [
                    { text: '📋 Мои заказы' },
                    { text: '💬 Поддержка' }
                ]
            ],
            resize_keyboard: true
        }
    });
});

// Обработка команды "Мои заказы"
bot.hears('📋 Мои заказы', (ctx) => {
    ctx.reply('🔮 Ваши заказы загружаются из магических записей...', {
        reply_markup: {
            inline_keyboard: [
                [{ text: '🌿 Открыть лавку', web_app: { url: `${PUBLIC_URL}/orders` } }]
            ]
        }
    });
});

// Обработка команды "Поддержка"
bot.hears('💬 Поддержка', (ctx) => {
    ctx.reply(`🧙‍♀️ Служители Эфирной Лавки всегда готовы помочь!

📞 Для связи с нашими магами:
• @DaryaDub_07 - Дарья, мастер трав и масел
• @Dan_vark - Алексей, знаток ритуальных практик

📧 Или напишите сюда свой вопрос, и мы ответим в течение дня.

✨ Да пребудет с вами магия!`);
});

// Команда /admin - доступ к админ-панели только для авторизованных пользователей
bot.command('admin', (ctx) => {
    const userId = ctx.from.id;
    const adminIds = process.env.ADMIN_IDS?.split(',').map(id => parseInt(id.trim())) || [985246360, 1562870920];
    
    if (!adminIds.includes(userId)) {
        ctx.reply(`🌫️ Пространство Это не терпит посторонних

Возвращайся на тропу, путник. Возможно, тебе нужен иной ключ — заговорённое слово, особая трава или разрешение самой Хранительницы Ароматов.`);
        return;
    }
    
    const userName = ctx.from.first_name || 'Хранитель';
    ctx.reply(`🔮 Приветствую тебя, ${userName}! ✨

Врата Алтаря Чародейки открываются перед тобой...

🧙‍♀️ Здесь ты можешь управлять магическим царством:
• Добавлять новые зелья и артефакты  
• Редактировать описания товаров
• Просматривать заказы странников
• Настраивать акции и предложения

Войди в священные чертоги администрирования:`, {
        parse_mode: 'Markdown',
        reply_markup: {
            inline_keyboard: [
                [{ 
                    text: '🧙‍♀️ Алтарь Чародейки', 
                    web_app: { url: `${PUBLIC_URL}/admin.html` } 
                }]
            ]
        }
    });
});

// Экспортируем запуск для server.js
export function initBot(app) {
    // Подключаем webhook
    app.use(bot.webhookCallback('/telegram/webhook'));

    // Устанавливаем webhook
    bot.telegram.setWebhook(`${PUBLIC_URL}/telegram/webhook`)
        .then(() => console.log('🌿 Webhook установлен для Эфирной Лавки:', `${PUBLIC_URL}/telegram/webhook`))
        .catch(console.error);
    
    console.log('🔮 Бот Эфирной Лавки инициализирован');
}

// Функция для отправки уведомлений в каналы
export async function sendChannelNotification(message, isTest = false) {
    try {
        const channelId = isTest ? '-1002277761715' : '-1002261187486';
        await bot.telegram.sendMessage(channelId, message, { parse_mode: 'Markdown' });
        return true;
    } catch (error) {
        console.error('Ошибка отправки в канал:', error);
        return false;
    }
}
