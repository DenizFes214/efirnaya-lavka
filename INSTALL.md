# Развёртывание на AMVERA — 🌿ЭфирнаяЛавка🌿

Домен: **efirnaya-lavka-aleksei57.amvera.io**  
Переменные окружения: в файле `.env`

## Шаги
1. Залейте репозиторий на AMVERA
2. Укажите runtime: Docker Compose
3. Добавьте переменные окружения из `.env.example`
4. Запустите деплой

## Переменные окружения
- `BOT_TOKEN` - токен Telegram бота от @BotFather
- `PUBLIC_URL` - публичный URL вашего приложения
- `ADMIN_IDS` - ID администраторов через запятую
- `PROMO_TARGET_CHAT_ID` - ID чата/канала для публикации акций
- `YOOKASSA_SHOP_ID` - ID магазина в ЮKassa (опционально)
- `YOOKASSA_SECRET_KEY` - секретный ключ ЮKassa (опционально)

## Docker
```
docker compose up -d --build
```


Сервисы:
- **backend**: Node.js API, статика `/uploads`, порт 8080
- **frontend**: nginx раздаёт `frontend` и проксирует `/api` на backend
- **db**: SQLite-файл в volume `./data`

## Пути
- Главная страница: `/`
- Админка: `/admin.html`
- API загрузки фото: `POST /api/admin/products/:id/images`
- Галерея товара: `GET /api/product/:id/images`

## Особенности реализации
1. Просмотр фото товара с галереей
2. Админ-меню через иконку "⋮" в правом верхнем углу
3. Система отзывов с модерацией
4. Новая структура меню (сначала категории, потом товары)
5. Управление разделами для админа
6. Загрузка изображений через файлы
7. Система доставки с разными вариантами
8. Уведомления админу о заказах
9. Система акций с публикацией в канале
10. Статистика заказов
11. Закреп сообщения в канале